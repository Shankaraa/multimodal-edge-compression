# Install the generic utilities first:
#   pip install -r requirements.txt
#
# Then, inside Linux or WSL2, install the runtime stack below. `vllm` is left as a manual
# install because the guide requires the nightly wheel and an extra index URL.

mistral_common[audio]>=1.9.0
transformers>=5.2.0
av>=17.0.0
auto-gptq
optimum
bitsandbytes
GPTQModel
llmcompressor

# Manual step for vLLM nightly:
# Prefer `uv` because the official vLLM docs recommend it for nightly wheels and CUDA selection.
# On CUDA 12.x L4 rentals, select the matching CUDA 12.x nightly wheel/index for the installed
# Torch backend instead of copying a machine-specific CUDA 13 command.
#   python -m pip install -U uv
#   uv pip install -U vllm --torch-backend=<cuda12x-backend> --extra-index-url <matching-vllm-nightly-index>
